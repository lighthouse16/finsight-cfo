# -----------------------------------------------------------------------------
# FinSight CFO Production Environment Configuration Template
# -----------------------------------------------------------------------------
# Copy this file to `.env.production` or set these in your server environment.

# 1. Base App Configuration
APP_MODE=production
ALLOW_DEMO_FALLBACK=false
MARKET_WATCH_USE_FIXTURES=false
CORS_ALLOW_ORIGINS=https://your-production-app.com,https://api.your-production-app.com

# 2. Persistence / Database Configuration
# Options: 'database' (production) or 'local' (development memory/json fallback)
PERSISTENCE_BACKEND=database
DATABASE_URL=postgresql://db_user:db_password@db_host:5432/db_name
DATABASE_ECHO=false

# 3. Authentication Configuration
# JWT_SECRET_KEY must be set to a secure, random string in production mode.
AUTH_MODE=production
JWT_SECRET_KEY=your-randomly-generated-32-character-secret-key-here
JWT_ALGORITHM=HS256
AUTH_DEFAULT_ORGANIZATION_ID=org_production_default
AUTH_DEFAULT_USER_ID=user_production_default
AUTH_DEFAULT_ROLE=viewer
AUTH_ALLOW_HEADER_OVERRIDES=false

# 4. Job Queue Configuration (Redis)
# Options: 'redis' (production Celery/worker broker) or 'in_process'
QUEUE_BACKEND=redis
QUEUE_REDIS_URL=redis://:redis_password@redis_host:6379/0

# 5. Object Storage Configuration (S3 / MinIO)
OBJECT_STORAGE_BACKEND=s3
S3_ENDPOINT_URL=https://s3.your-region.amazonaws.com
S3_BUCKET=your-production-s3-bucket-name
S3_ACCESS_KEY_ID=your-s3-access-key-id
S3_SECRET_ACCESS_KEY=your-s3-secret-access-key
S3_REGION=us-east-1
S3_SECURE=true
S3_FORCE_PATH_STYLE=true

# 6. External API / Provider Settings
FX_PROVIDER=frankfurter
FX_PROVIDER_BASE_URL=https://api.frankfurter.dev/v2
ALPHA_VANTAGE_API_KEY=your-alpha-vantage-api-key
CHINADATA_API_KEY=your-chinadata-api-key
IHS_API_KEY=your-ihs-markit-api-key
FEDWATCH_API_KEY=your-fedwatch-api-key
RMB_BENCHMARK_API_KEY=your-rmb-benchmark-api-key
LENDER_CATALOG_PATH=/var/lib/finsight/provider_catalog.json
COMMODITY_PROVIDER=fixture
HTTP_TIMEOUT_SECONDS=15

# 7. Cache Configuration
rates_ttl_seconds=21600
liquidity_ttl_seconds=21600

# 8. Frontend Vite Config
# Leave empty for same-origin (/api) routing through Nginx, or set to your API domain.
VITE_API_BASE_URL=
